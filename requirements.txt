# requirements.txt
# Core dependencies for Pete-Eebot

# HTTP requests to APIs (Withings, Wger, Telegram)
requests>=2.31.0

# Optional: time zone support for scheduling/analytics
python-dateutil>=2.8.2

pydantic-settings==2.3.0

psycopg[binary]==3.1.18